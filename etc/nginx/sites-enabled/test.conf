server {
	error_log /home/box/web/log/error_log info;
	listen 80;
	location ^~ /uploads/ {
		root /home/box/web;
	}
	location ~* ^.+\.\w+${
		root /home/box/web/public;
	}
	location / {
		return 404;
	}
}